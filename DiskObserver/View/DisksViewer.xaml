<UserControl x:Class="DiskObserver.View.DisksViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:view="clr-namespace:DiskObserver.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <view:SizeConverter x:Key="SizeConverter"/>
    </UserControl.Resources>

    <ListBox ItemsSource="{Binding PhysicalDisks}" SelectedItem="{Binding SelectedPhysicalDisk}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
        <ListBox.ItemTemplate>
            <DataTemplate>

                <Grid Margin="5,0,2,0">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="{Binding Name}"/>
                    <TextBlock Grid.Column="1" Text="{Binding Format}"/>
                    <TextBlock Grid.Column="2" Text="{Binding FreeMemory, Converter={StaticResource SizeConverter}}"/>
                    <TextBlock Grid.Column="3" Text="{Binding TotalMemory, Converter={StaticResource SizeConverter}}"/>

                </Grid>

            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
